<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="board">
	<select id="boardList" parameterType="b" resultType="b">
		select 
			board_no as boardNo,
			board_title as boardTitle,
			board_content as boardContent,
			board_writer as boardWriter,
			readCount,
			regDate,
			priority,
			fileName,
			filePath,
			ncCount
		from board
	</select>
	
	<select id="boardView" parameterType="b" resultType="b">
		select
			board_no as boardNo,
			board_title as boardTitle,
			board_content as boardContent,
			board_writer as boardWriter,
			readCount,
			regDate,
			priority,
			fileName,
			filePath,
			ncCount
		from board where board_no=#{boardNo}
	</select>
	
	<update id="boardHit" parameterType="b">
		update board set readCount=readCount+1 where board_no=#{boardNo}
	</update>
	
	<update id="boardUpdate" parameterType="b">
		update board set board_title=#{boardTitle}, board_content=#{boardContent} where board_no=#{boardNo}
	</update>
	
	<delete id="boardDelete" parameterType="int">
		delete from board where board_no=#{boardNo}
	</delete>
	
	<insert id="boardWrite" parameterType="b">
		INSERT INTO board VALUES
			(BOARD_SEQ.NEXTVAL,#{boardTitle},#{boardContent},#{boardWriter},#{readCount},#{regDate},#{priority},#{fileName},#{filePath},#{ncCount})
	</insert>
	
</mapper>



















